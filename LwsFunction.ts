{


    // function in typeScript


    const typeBaby = (x: number, y: number, z: number) =>{
            
          const valueZIs = x + y + z 

          const all = valueZIs * 700 

          const ofVlaueIs = all / 200 

          const allIs = ofVlaueIs - 10 

          console.log(allIs)

          if( allIs > 50 ){

            console.log('type is good')

          } else {

            console.log('type is not good')

          }
 

    }


    typeBaby(23, 24, 232)













}